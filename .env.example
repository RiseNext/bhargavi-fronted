# Copy to .env.local for local work. On Vercel, set these under
# Project → Settings → Environment Variables.

# Canonical origin — drives metadataBase, canonical tags, sitemap.xml and
# robots.txt. No trailing slash. Leave unset to use the production domain
# baked into src/lib/site.ts.
# NEXT_PUBLIC_SITE_URL=https://www.bhargavihealthworld.com

# --- Not wired up yet -------------------------------------------------------
# The appointment / contact forms POST to /api/contact, which currently only
# validates and logs. Nothing is delivered. Before launch, pick a provider and
# add its key here plus the delivery call in src/app/api/contact/route.ts.
# RESEND_API_KEY=
# CONTACT_TO_EMAIL=bhargavihealthworld@gmail.com
